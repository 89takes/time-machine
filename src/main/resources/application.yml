spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/timemachine
    username: divijdua
    password:
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true

  jackson:
    serialization:
      INDENT_OUTPUT: true

server:
  port: 8080
